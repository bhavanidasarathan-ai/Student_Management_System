const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="🔍 Search Student..."
      style={{
        width: "300px",
        padding: "10px",
        borderRadius: "8px",
        border: "1px solid #ccc",
        marginBottom: "20px",
      }}
    />
  );
};

export default SearchBar;
