const Loader = () => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "20px",
        padding: "30px",
      }}
    >
      Loading...
    </div>
  );
};

export default Loader;
